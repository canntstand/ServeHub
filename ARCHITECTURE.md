- new: сделал структуру базовых файлов
- Добавил .env.example для примеров данных, .gitignore, ARCHITECTURE.md для записей по проекту, Cheatsheet.md для уточнений по ведению проекта, завершил README.md который олицетворяет проект
---
- wip: сделал первые конфиги docker-compose, перешел на gitlab CI, небольшие изменения в некоторых файлах
- Решил перейти на GitLab CI по причине того что мой будущий сервер будет работать локально (не на VDS или VPS) и из-за этого могут возникать проблемы с CD частью, так как GitHub не смог бы подключиться к серверу. В случае GitLab я установлю у себя раннер в отдельном контейнере и чтобы его запускать установлю образ gitlab-ce.
- Написал первую конфигурацию docker-compose.dev.yaml для разработки: пока что только база данных, приложение, pgadmin4 и gitlab
- Создал run.py чтобы каждый раз не печатать docker-compose команды
---
wip: написал docker-compose для gitlab-ce
- Из важного решил не использовать сервисы по типу postfix, а решил работать с помощью своей гугл почты и smtp встроенного gitlab, так как я хочу реализовать все максимально "из коробки" без покупки доменов или чего-то подобного. Еще ничего не тестировал, поэтому возможно прийдется менять тактику
---
wip: добился рабочего gitlab и других контейнеров
- Оказалось проблема в работе gitlab заключалась в ненастроенном nginx
---
wip: получил доступ к root пользователю, но все еще нужно настроить автоматизацию
- Получилось поменять пароль root и тем самым зайти в gitlab, теперь нужно сделать скрипт для автоматизации смены root пароля
---
wip: добавил .sh скрипт для быстрой настройки виртуального окружения
- Скрипт создает alias для работы в докер и скачивает зависимости из .txt файла, потом можно будет дополнить
---
wip: добавил .ps1 скрипта для инициализации виртуального окружения
- Скрипт адаптирован для powershell, выполняет те же функции
---
ref: улучшения в структуре
- Разметил структуру сервисов, сменил назавние папки, добавил INSTALLATION_GUIDE.md
---
wip: в процессе автоматизации файлов для создания окружения
- Начал редактировать .sh файл, пока нерабочий
---
feat: добавлены 2 рабочих скрипта по автоматической инициализации python окружения
- 2 скрипта автоматически создают окружение и  читают .txt файлы из сервисов из скачивают все зависимости. Один скрипт - .sh, другой - .ps1
---
wip: изменения в docker-compose.dev, изменения в root-gitlab-setup, пока подготовка
- убрал лишний код в файлах, разбирался с предполагаемой архитектурой
---
fix: smtp в gitlab ce теперь работает нормально, к изменению пароля root добавилось изменение почты
- Была проблема с подключением SMTP - проблема решилась, также была проблема с отсутствием сообщения о подтверждении изменения почты у аккаунта, решил обходным путем через установку почты заранее. Если root не нужен можно также создать новый аккаунт для своей почты, но нужно все равно будет сначала зайти в root
---
wip: добавил настройку gitlab runner
- Настроил runner к проекту и добавил файл для автоматической настройки
---
wip: Тестовый ci файл
---
wip: Проверка тестового ci 2
---
fix: исправил ошибки и настроил работу runner для базового CI
- Была ошибка с обращением по url не в docker сети, а по localhost, из-за чего и была ошибка, также исправил ошибку с обращением к неверному имени в docker сети.
- Добавил автоматическое отключение разрешения регистрации другим пользователям ради безопасности
---
ref: дописал вспомогательные .md файлы
---